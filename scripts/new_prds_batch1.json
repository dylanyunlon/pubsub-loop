[
  {
    "title": "[EPIC] io模块个体状态序列化引擎——支持Schema-aware增量编码与跨ABI版本化传输格式",
    "module": "io",
    "priority": "P0",
    "sprint": "Current",
    "status": "Todo",
    "claude": "Claude-A",
    "language": "C++/CUDA",
    "body": "## 架构定位\n\n| 维度 | 说明 |\n|------|------|\n| **所属层** | Layer 1 — 世界模型层（World Model） |\n| **模块** | io |\n| **角色** | 为 pub/sub-loop 世界的个体状态传输提供**统一的序列化/反序列化基础设施**。io 模块负责将内存中的 `IndividualState` 结构转换为字节流用于 RTPS/Shm 传输，以及从字节流恢复为类型安全的状态对象 |\n| **延迟链位置** | 序列化在 `publish()` 热路径上；反序列化在 `subscribe()` 接收路径上。64KB 载荷目标: 序列化 ≤ 50µs，反序列化 ≤ 50µs |\n| **数据流位置** | 个体 `publish(state)` → io::Serializer → transport → io::Deserializer → subscriber `on_message(state)` |\n\n### 个体交互说明\n\nio 模块是个体间**状态传输的编解码器**——每次个体发布状态，io 将 C++ 内存结构转为传输格式；每次订阅者接收状态，io 将传输格式恢复为类型安全对象。\n\n### Current API Surface\n\n```cpp\n// io/include/io/serializer.h（当前）\nnamespace world::io {\n  class Serializer {\n  public:\n    // 当前: 硬编码的 memcpy 序列化，不支持版本化\n    template <typename T>\n    std::vector<uint8_t> serialize(const T& state);\n\n    template <typename T>\n    T deserialize(std::span<const uint8_t> bytes);\n    // 缺少: schema awareness, 增量编码, 版本号\n  };\n}\n```\n\n### 问题陈述\n\n1. **无 Schema 感知**: 序列化使用 `memcpy`，字段增减导致接收方解析失败\n2. **无增量编码**: 每次传输全量状态，即使只有 position.x 变化也传输整个 2KB 结构\n3. **无版本化**: 发布方和订阅方编译的 `IndividualState` 版本不同时，直接 crash\n4. **全量传输带宽浪费**: 100K 个体 × 256B × 30Hz = 768MB/s，增量编码可降至 <100MB/s\n\n### Proposed Design\n\n#### Schema-aware 序列化器\n\n```cpp\nnamespace world::io {\n\n// 编译期 schema 描述\ntemplate <typename T>\nstruct StateSchema {\n    static constexpr uint32_t version = 1;\n    static constexpr auto fields = make_field_list(\n        Field{\"pos\",    &T::pos,    FieldType::Vec3f},\n        Field{\"vel\",    &T::vel,    FieldType::Vec3f},\n        Field{\"attrs\",  &T::attrs,  FieldType::FloatArray},\n        Field{\"parent\", &T::parent, FieldType::IndividualId}\n    );\n};\n\n// 增量编码器\ntemplate <typename T>\nclass DeltaEncoder {\npublic:\n    // 首次: 编码全量快照\n    std::vector<uint8_t> encode_snapshot(const T& state);\n\n    // 后续: 仅编码与上次不同的字段\n    // 返回 field_mask + changed_fields_data\n    std::vector<uint8_t> encode_delta(const T& current, const T& previous);\n\n    // 解码: 基于 field_mask 部分更新\n    T decode_delta(const T& base, std::span<const uint8_t> delta_bytes);\n\nprivate:\n    // field_mask: 每个字段 1 bit，标记是否变化\n    uint64_t compute_field_mask(const T& a, const T& b) const;\n};\n\n// 版本化传输头\nstruct WireHeader {\n    uint32_t schema_version;  // 发布方 schema 版本\n    uint32_t payload_size;    // 载荷字节数\n    uint8_t  encoding;        // 0=snapshot, 1=delta\n    uint64_t field_mask;      // 哪些字段被编码\n    uint64_t sequence;        // 单调递增序列号\n};\n}\n```\n\n#### 版本兼容矩阵\n\n```cpp\n// 版本协商: 订阅方检查 schema_version\nenum class CompatResult {\n    Exact,      // 版本完全相同\n    Forward,    // 发布方版本更新，新字段被忽略\n    Backward,   // 发布方版本更旧，缺失字段用默认值\n    Incompatible // 不兼容（breaking change）\n};\n\nCompatResult check_compat(uint32_t publisher_ver, uint32_t subscriber_ver);\n```\n\n## Dependencies\n\n| 依赖项 | 原因 | 状态 |\n|--------|------|------|\n| `message::IndividualState` | 被序列化的核心类型 | 可用 |\n| transport 模块 | 序列化输出的消费者 | 可用 |\n| [FEA] Message模块constexpr化 | schema 字段描述需要 constexpr | 同步开发 |\n| [BUG] C-ABI Wire类型 | Wire 类型是序列化的目标格式 | 同步开发 |\n\n## Sub-tasks\n\n| # | 任务 | 优先级 | 估计工时 |\n|---|------|--------|---------|\n| 1 | 定义 `StateSchema<T>` 编译期 schema 描述框架 | P0 | 3d |\n| 2 | 实现 `DeltaEncoder<T>::encode_delta()` 增量编码 | P0 | 5d |\n| 3 | 实现 `WireHeader` 版本化传输头 | P0 | 2d |\n| 4 | 实现版本兼容检查 `check_compat()` | P1 | 2d |\n| 5 | 为 `IndividualState` / `IndividualPos` 实现 schema 注册 | P0 | 2d |\n| 6 | 基准测试: 全量 vs 增量编码吞吐对比 | P1 | 2d |\n| 7 | 集成到 `transport::ChannelWriter<T>` 的序列化路径 | P0 | 3d |\n| 8 | 跨版本往返测试: v1→v2→v1 无数据丢失 | P1 | 2d |\n\n## Acceptance Criteria\n\n- [ ] 增量编码仅传输变化字段: 单字段变化时传输量 < 全量的 10%\n- [ ] 序列化延迟: 64KB IndividualState ≤ 50µs (x86), ≤ 100µs (ARM)\n- [ ] 版本前向兼容: v1 订阅方可正确解析 v2 发布方的数据(新字段被忽略)\n- [ ] 版本后向兼容: v2 订阅方可正确解析 v1 发布方的数据(缺失字段用默认值)\n- [ ] `field_mask` 正确标记变化字段, 零 false positive\n- [ ] 100K 个体场景下, 增量编码将网络带宽降至全量方案的 15% 以下\n- [ ] Schema 注册宏 `WORLD_STATE_SCHEMA(T, fields...)` 零运行时开销\n"
  },
  {
    "title": "[FEA] common模块世界时钟同步原语——跨节点TickClock对齐与漂移校正机制",
    "module": "common",
    "priority": "P1",
    "sprint": "Current",
    "status": "Todo",
    "claude": "Claude-D",
    "language": "C++/CUDA",
    "body": "## 架构定位\n\n| 维度 | 说明 |\n|------|------|\n| **所属层** | Layer 1 — 世界模型层（World Model） |\n| **模块** | common |\n| **角色** | 为分布式 pub/sub-loop 世界提供**统一的逻辑时钟**。当多个节点运行同一世界的个体时，每个节点的本地 tick 计数器会因 CPU 频率差异、调度抖动而漂移。common::TickClock 提供跨节点的 tick 对齐协议 |\n\n### 问题陈述\n\n在分布式世界中，节点 A 的 tick 42 和节点 B 的 tick 42 不一定对应同一物理时刻。当个体从节点 A 迁移到节点 B 时，tick 不对齐导致：\n\n1. **状态融合错位**: fusion engine 将不同物理时刻的状态混合\n2. **因果违反**: 个体 B 响应了个体 A 尚未发生的事件\n3. **回放不可重现**: 录制的 tick 序列在不同节点回放时产生不同结果\n\n### Current API Surface\n\n```cpp\n// common/include/common/tick_clock.h（当前）\nnamespace world::common {\n  class TickClock {\n  public:\n    WorldTick current_tick() const;    // 本地 tick（无同步）\n    void advance();                     // tick++\n    Duration tick_duration() const;     // 目标 tick 间隔（如 33ms）\n    // 缺少: 跨节点同步、漂移检测、NTP-like 校正\n  };\n}\n```\n\n### Proposed Design\n\n```cpp\nnamespace world::common {\n\nstruct ClockSyncConfig {\n    Duration target_tick_duration = 33ms;     // 目标 tick 间隔\n    Duration max_drift_tolerance = 5ms;       // 最大可容忍漂移\n    size_t sync_interval_ticks = 100;         // 每 N ticks 同步一次\n    float drift_correction_rate = 0.1f;       // 校正速率 (0-1)\n};\n\nclass SynchronizedTickClock {\npublic:\n    explicit SynchronizedTickClock(ClockSyncConfig cfg);\n\n    // 本地 tick (同步校正后)\n    WorldTick current_tick() const;\n\n    // 带时间戳的 tick 推进\n    void advance(Timestamp wall_clock_now);\n\n    // --- 同步协议 ---\n    // 主节点广播自己的 (tick, timestamp) 对\n    SyncBeacon generate_beacon() const;\n\n    // 从节点接收 beacon 并计算漂移\n    void apply_beacon(const SyncBeacon& beacon);\n\n    // 当前漂移量 (正=本地快, 负=本地慢)\n    Duration estimated_drift() const;\n\n    // 同步质量指标\n    struct SyncMetrics {\n        Duration current_drift;\n        Duration max_observed_drift;\n        uint64_t corrections_applied;\n        uint64_t sync_beacons_received;\n        float clock_quality;  // 1.0 = 完美同步\n    };\n    SyncMetrics metrics() const;\n};\n\nstruct SyncBeacon {\n    WorldTick tick;\n    Timestamp wall_clock;    // 主节点的本地墙钟时间\n    uint64_t beacon_seq;     // beacon 序列号\n    Duration round_trip;     // 可选: 用于补偿网络延迟\n};\n\n}  // namespace world::common\n```\n\n### 同步协议\n\n```\n主节点 (tick master)                    从节点\n    │                                      │\n    ├── SyncBeacon{tick=1000, ts=T0} ─────►│\n    │                                      ├── 计算漂移 = local_tick - 1000\n    │                                      ├── 校正: tick_rate *= (1 - drift * correction_rate)\n    │                                      │\n    │   ... 100 ticks later ...            │\n    │                                      │\n    ├── SyncBeacon{tick=1100, ts=T1} ─────►│\n    │                                      ├── 更新漂移估计\n    │                                      │\n```\n\n## Dependencies\n\n| 依赖项 | 原因 |\n|--------|------|\n| transport 模块 | SyncBeacon 通过 pub/sub 通道广播 |\n| service_discovery | 确定哪个节点是 tick master |\n| profiler | 漂移指标上报 |\n\n## Sub-tasks\n\n| # | 任务 | 优先级 |\n|---|------|--------|\n| 1 | 实现 `SynchronizedTickClock` 基础结构 + `advance()` | P0 |\n| 2 | 实现 `SyncBeacon` 生成与广播（主节点侧） | P0 |\n| 3 | 实现 `apply_beacon()` 漂移检测与校正（从节点侧） | P0 |\n| 4 | 实现网络延迟补偿（RTT/2 校正） | P1 |\n| 5 | 集成到 `scheduler::TickLoop` 的 tick 推进逻辑 | P0 |\n| 6 | 压力测试: 10 节点漂移收敛时间 | P1 |\n\n## Acceptance Criteria\n\n- [ ] 10 节点场景下，tick 漂移收敛到 ≤ 2ms（100 个 sync beacon 内）\n- [ ] 单节点 CPU 负载突增 50% 时，tick 漂移不超过 `max_drift_tolerance`\n- [ ] SyncBeacon 带宽开销 < 1KB/s（每 100 tick 一个 beacon）\n- [ ] 主节点故障时，从节点自动选举新主节点（通过 service_discovery）\n- [ ] `SyncMetrics::clock_quality` 在稳态下 ≥ 0.95\n"
  },
  {
    "title": "[FEA] component模块GPU个体批处理调度器——将同类个体tick逻辑合并为CUDA kernel批量执行",
    "module": "component",
    "priority": "P0",
    "sprint": "Current",
    "status": "Todo",
    "claude": "Claude-D",
    "language": "C++/CUDA",
    "body": "## 架构定位\n\n| 维度 | 说明 |\n|------|------|\n| **所属层** | Layer 2 — 内部决策层（Decision Engine） |\n| **模块** | component |\n| **角色** | 将同类型个体的 tick 更新逻辑合并为 CUDA kernel 批量执行，消除逐个体 kernel launch 开销 |\n\n### 问题陈述\n\n当前每个个体的 `on_tick()` 方法独立执行。对于 100K 个 SensorFusion 类型个体，调度器启动 100K 次独立的 tick 回调。在 GPU 世界状态处理器上，这意味着 100K 次 kernel launch，每次 launch 开销 ~5µs，仅 launch 开销就占 500ms——远超 33ms 的 tick 预算。\n\n### Current API Surface\n\n```cpp\n// component/include/component/component_base.h（当前）\nnamespace world::component {\n  class ComponentBase {\n  public:\n    // 当前: 逐个体调用\n    virtual void on_tick(WorldTick tick) = 0;\n    // 100K 个体 = 100K 次虚函数调用 + 100K 次 kernel launch\n  };\n}\n```\n\n### Proposed Design\n\n```cpp\nnamespace world::component {\n\n// GPU 批处理接口: 同类个体合并为一次 kernel launch\ntemplate <typename IndividualStateT>\nclass BatchTickProcessor {\npublic:\n    struct BatchConfig {\n        size_t max_batch_size = 65536;      // 单次 kernel 最大个体数\n        size_t min_batch_size = 256;        // 低于此阈值走 CPU 标量路径\n        bool enable_warp_specialization = true;  // sm_90+ 异步 warp 分工\n    };\n\n    explicit BatchTickProcessor(BatchConfig cfg);\n\n    // 注册批处理 kernel\n    // kernel 签名: void(IndividualStateT* states, size_t count, WorldTick tick)\n    using BatchKernel = void(*)(IndividualStateT*, size_t, WorldTick);\n    void set_kernel(BatchKernel kernel);\n\n    // 批量执行: 将 N 个个体的状态合并到连续 GPU buffer，一次 launch\n    void execute_batch(\n        std::span<IndividualStateT*> individual_states,\n        WorldTick tick,\n        cudaStream_t stream\n    );\n\n    // 性能指标\n    struct BatchMetrics {\n        uint64_t total_batches;\n        uint64_t total_individuals_processed;\n        Duration avg_batch_launch_time;\n        Duration avg_kernel_time;\n        float gpu_occupancy;            // SM 利用率\n        size_t avg_batch_size;\n    };\n    BatchMetrics metrics() const;\n};\n\n// 使用示例\n__global__ void sensor_fusion_batch_tick(\n    IndividualState* states, size_t count, WorldTick tick)\n{\n    size_t idx = blockIdx.x * blockDim.x + threadIdx.x;\n    if (idx >= count) return;\n\n    // 每个线程处理一个个体的 tick 逻辑\n    auto& state = states[idx];\n    state.pos.x += state.vel.x * dt;\n    state.pos.y += state.vel.y * dt;\n    state.pos.z += state.vel.z * dt;\n    // ... fusion logic ...\n}\n\n}  // namespace world::component\n```\n\n### Before / After\n\n| 维度 | Before | After |\n|------|--------|-------|\n| 100K 个体 tick | 100K kernel launches (500ms) | 2 kernel launches (0.5ms) |\n| GPU 利用率 | ~3% (launch-bound) | ~85% (compute-bound) |\n| 内存布局 | AoS (array of structs) | SoA 可选 (struct of arrays) |\n| 个体类型切换 | 逐个体虚函数 | 按类型分组批处理 |\n\n## Dependencies\n\n| 依赖项 | 原因 |\n|--------|------|\n| scheduler::CRoutineScheduler | 批处理调度集成 |\n| transport::ZeroCopyRegion | 批量状态缓冲 |\n| CUDA >= 12.0 | Warp specialization (sm_90+) |\n\n## Sub-tasks\n\n| # | 任务 | 优先级 |\n|---|------|--------|\n| 1 | 实现 `BatchTickProcessor` 框架 + GPU buffer 管理 | P0 |\n| 2 | 实现同类个体状态收集到连续 GPU buffer | P0 |\n| 3 | 实现批量 kernel launch + 结果写回 | P0 |\n| 4 | 实现 AoS→SoA 转换层（可选优化） | P1 |\n| 5 | 基准测试: 100K 个体批处理 vs 逐个体 | P0 |\n| 6 | sm_90+ warp specialization 路径 | P2 |\n\n## Acceptance Criteria\n\n- [ ] 100K 同类个体 tick 处理时间 < 5ms（vs 当前 500ms）\n- [ ] GPU SM 利用率 ≥ 70% (100K 个体)\n- [ ] 批处理与逐个体处理产生 bit-identical 状态结果\n- [ ] 自动按个体类型分组: N 种类型 = N 次 kernel launch\n- [ ] 低于 `min_batch_size` 的类型自动回退到 CPU 标量路径\n"
  },
  {
    "title": "[EPIC] Layer 3 Needle Engine 集成规范——pub/sub-loop 世界状态到 Web 3D 实时渲染全链路",
    "module": "tools",
    "priority": "P1",
    "sprint": "Next",
    "status": "Todo",
    "claude": "Claude-E",
    "language": "Mixed",
    "body": "## 架构定位\n\n| 维度 | 说明 |\n|------|------|\n| **所属层** | Layer 3 — 展示层（Presentation） |\n| **角色** | 将 pub/sub-loop 世界模型中个体的实时状态通过 needle-tools/needle-engine 在 Web 端渲染为 3D 场景 |\n\n### 展示层概述\n\nNeedle Engine (github.com/needle-tools) 是一个 Web 端 3D 运行时，支持：\n- 浏览器中加载和渲染 GLB/glTF 资产\n- WebXR (VR/AR) 跨平台部署\n- 基于 three.js 的渲染管线\n- @needle-tools/gltf-progressive 实现 LOD 渐进加载\n\n**关键约束**: 展示层只是 pub/sub-loop 的一个输出模块。不是所有模块都属于展示——transport、scheduler、croutine 等是后端基础设施。展示层消费 Layer 2 的输出，不参与世界模型计算。\n\n### 端到端数据流\n\n```\nLayer 1 (World Model)\n  个体 publish(state) → transport → subscriber\n\nLayer 2 (Decision Engine)\n  fusion engine → profiler → statistics → data::WorldView\n\nLayer 3 (Presentation)\n  StateBridge → WebSocket → needle-engine (browser)\n    → GLB loader (gltf-progressive)\n    → three.js scene graph\n    → WebGL/WebGPU renderer\n    → 用户看到 3D 场景\n```\n\n### 核心组件清单\n\n| 组件 | PRD | 职责 |\n|------|-----|------|\n| State Bridge | L3-001 | C++ WorldView → WebSocket DeltaFrame |\n| DeltaFrame 协议 | L3-002 | 增量编码 + field_mask 压缩 |\n| GLB Asset Manifest | L3-003 | 个体类型 → GLB 模型映射 |\n| 客户端插值引擎 | L3-004 | velocity 外推 + slerp 旋转平滑 |\n| WebSocket 管理 | L3-005 | 重连 · 帧同步 · 背压 |\n| 可见性筛选 | L3-006 | 视锥剔除 + LOD 距离降级 |\n| GLB 占位体 | L3-007 | 模型加载中过渡显示 |\n| LRU 卸载 | L3-008 | GPU 内存管理 |\n| 性能面板 | L3-009 | FPS · 延迟 · 个体数显示 |\n| WebRTC 备选 | L3-010 | P2P 个体直连场景 |\n\n### Needle Engine 集成架构\n\n```javascript\n// 客户端核心循环 (needle-engine)\nimport { useNeedleProgressive } from \"@needle-tools/gltf-progressive\";\n\nclass PubSubWorldRenderer {\n    constructor(scene, wsUrl) {\n        this.scene = scene;\n        this.ws = new WebSocket(wsUrl);\n        this.individuals = new Map();  // id → three.js Object3D\n        this.interpolator = new StateInterpolator();\n    }\n\n    onDeltaFrame(frame) {\n        for (const update of frame.updates) {\n            let obj = this.individuals.get(update.id);\n            if (!obj) {\n                // 新个体: 加载 GLB 模型\n                obj = await this.loadGLB(update.type);\n                this.individuals.set(update.id, obj);\n                this.scene.add(obj);\n            }\n            // 插值目标更新\n            this.interpolator.setTarget(update.id, {\n                position: update.pos,\n                rotation: update.orient,\n                velocity: update.vel\n            });\n        }\n        for (const removed of frame.removed) {\n            this.scene.remove(this.individuals.get(removed));\n            this.individuals.delete(removed);\n        }\n    }\n\n    update(dt) {\n        // 每帧插值\n        for (const [id, obj] of this.individuals) {\n            const interp = this.interpolator.interpolate(id, dt);\n            obj.position.copy(interp.position);\n            obj.quaternion.copy(interp.rotation);\n        }\n    }\n}\n```\n\n### 性能预算\n\n| 环节 | 预算 | 说明 |\n|------|------|------|\n| StateBridge → WebSocket | ≤ 10ms | 服务端序列化+发送 |\n| 网络传输 | ≤ 20ms | WebSocket/局域网 |\n| 客户端解析 | ≤ 5ms | DeltaFrame 解码 |\n| three.js 渲染 | ≤ 16ms | 60fps 目标 |\n| **总计** | **≤ 50ms** | 端到端延迟 |\n\n### 个体可视化映射\n\n```yaml\n# asset_manifest.yaml\nindividual_types:\n  - type: \"world::sensor::LidarSensor\"\n    glb: \"assets/lidar_sensor.glb\"\n    lod_levels: 3\n    update_rate: 30Hz\n\n  - type: \"world::robot::ArmController\"\n    glb: \"assets/robot_arm.glb\"\n    lod_levels: 4\n    animation: \"joint_state\"  # 关节状态驱动骨骼动画\n\n  - type: \"world::vehicle::AutonomousCar\"\n    glb: \"assets/car.glb\"\n    lod_levels: 3\n    trails: true  # 显示运动轨迹\n```\n\n## Dependencies\n\n| 依赖项 | 原因 |\n|--------|------|\n| L3-001 ~ L3-010 子 PRD | 各组件的详细设计 |\n| data::WorldView | Layer 2 输出接口 |\n| needle-engine npm 包 | Web 3D 运行时 |\n| @needle-tools/gltf-progressive | GLB 渐进加载 |\n\n## Acceptance Criteria\n\n- [ ] 1000 个体场景在 Chrome 中稳定 60fps (MacBook Pro M2)\n- [ ] 端到端延迟（publish → 屏幕可见）≤ 100ms\n- [ ] GLB 资产使用 gltf-progressive LOD: 远处个体用低模，近处自动切换高模\n- [ ] WebSocket 断线后 3s 内自动重连，重连后 1s 内恢复场景一致性\n- [ ] 支持 ≥ 10 个并发浏览器客户端\n- [ ] 移动端 (iOS Safari) 可正常显示（降低个体数/LOD）\n"
  },
  {
    "title": "[FEA] scheduler模块BlockLoadTask预取优化——基于个体拓扑距离的CRoutine工作窃取亲和性",
    "module": "scheduler",
    "priority": "P1",
    "sprint": "Current",
    "status": "Todo",
    "claude": "Claude-A",
    "language": "C++/CUDA",
    "body": "## 架构定位\n\n| 维度 | 说明 |\n|------|------|\n| **所属层** | Layer 2 — 内部决策层 |\n| **模块** | scheduler |\n| **角色** | 优化 CRoutine 工作窃取调度器的缓存局部性。当前工作窃取是随机选择 victim core，不考虑被窃取的 CRoutine 所处理的个体与当前 core 上个体的拓扑距离 |\n\n### 问题陈述\n\npub/sub-loop 世界中，拓扑上相邻的个体（如同一空间区域的传感器群）频繁互相订阅。当 core 0 处理个体 A 后，个体 B（A 的邻居，订阅了 A 的状态）的 CRoutine 如果被 core 7 窃取执行，core 7 必须从 core 0 的 L2 cache 拉取 A 的状态——cache miss。如果 B 留在 core 0 执行，A 的状态已在 L1 cache 中。\n\n### Proposed Design\n\n```cpp\nnamespace scheduler {\n\nclass TopologyAwareWorkStealing {\npublic:\n    struct Config {\n        float locality_weight = 0.7f;     // 倾向选择拓扑近邻\n        float load_balance_weight = 0.3f;  // 负载均衡权重\n        size_t steal_batch_size = 4;       // 每次窃取的 CRoutine 数\n    };\n\n    // 选择窃取目标: 优先窃取与本 core 上个体拓扑相邻的 CRoutine\n    int select_victim(int thief_core, std::span<const CoreState> all_cores) const;\n\n    // 批量窃取: 一次窃取多个拓扑相邻的 CRoutine\n    std::vector<CRoutine*> steal_batch(int victim_core, int thief_core);\n\nprivate:\n    // 个体拓扑距离缓存 (world graph hop count)\n    TopologyDistanceCache topo_cache_;\n};\n\n}\n```\n\n## Sub-tasks\n\n| # | 任务 | 优先级 |\n|---|------|--------|\n| 1 | 实现 `TopologyDistanceCache`: 个体间 hop count 缓存 | P0 |\n| 2 | 实现 `select_victim()`: 加权选择（locality + load balance） | P0 |\n| 3 | 实现批量窃取: 一次窃取拓扑相邻的 N 个 CRoutine | P1 |\n| 4 | 基准: L2 cache miss rate before/after | P0 |\n\n## Acceptance Criteria\n\n- [ ] L2 cache miss rate 降低 ≥ 30% (100K 个体, 8 cores)\n- [ ] 工作窃取选择 victim 的延迟 < 100ns\n- [ ] 负载均衡不退化: 最大 core 负载差 < 15%\n"
  }
]