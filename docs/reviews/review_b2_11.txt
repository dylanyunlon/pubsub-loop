## pub/sub-loop PRD审核

---

**Q1 架构定位** ✅

世界模型定位明确，`world::tools` 命名空间清晰，多平台覆盖（x86/ARM/嵌入式）有交代。但"模块: ?"未填写是硬伤——P1 bug 连模块归属都空缺，分流时会卡住。

---

**Q2 问题陈述** ❌

标题点名了 `radix_sort` 在 T-4 runner 上失败，但正文完全没有跟进：

- 失败的具体表现是什么（crash / wrong output / timeout）？
- T-4 与其他 runner 的差异（硬件架构、内存配置、并发度）？
- 复现率是多少？是否有日志或 CI 链接？

正文大量篇幅在描述系统背景，对"为什么 radix_sort 在 T-4 上坏掉"零解释，问题陈述缺失。

---

**Q3 个体交互** ✅ / ⚠️

表格结构清晰，三类个体角色与交互方式已区分，channel URI 也给出。

弱点：`SchedulerIndividual` 的 CRoutine 机制与 radix_sort 失败的关联未说明——如果 bug 跟调度时序有关，这里应补充。

---

**Q4 API 设计** ❌

API surface 展示了 `Processor` 接口，但：

- `process_tick` 返回 `StateView`，其生命周期由谁管理（borrowed / owned）？
- `StateBuffer` 的线程安全保证未标注——T-4 是多线程 runner，这是关键；
- `radix_sort` 在哪里？API 里完全看不到被 bug 点名的函数，修复目标与接口设计脱节；
- `from_json` 的 schema 未给出，集成方无法对接。

---

**Q5 展示层影响** ❌

文档完全缺失此部分。"个体动态输出场景验证通过"在 AC 里一笔带过，但没有说明：

- GLB 动态输出在 radix_sort 失败时的降级行为或视觉异常；
- 监控看板（MonitorIndividual 诊断通道）的可观测性指标定义；
- 对下游消费者（订阅 `channel://world/tools/update` 的端）是否有破坏性变更。

---

**总结**

| 维度 | 结论 | 核心缺口 |
|---|---|---|
| Q1 架构定位 | ✅ | 模块字段空缺需补 |
| Q2 问题陈述 | ❌ | 无复现步骤、无失败现象、无环境差异分析 |
| Q3 个体交互 | ✅⚠️ | 调度与 bug 的关联未说明 |
| Q4 API 设计 | ❌ | radix_sort 不在 API 里、生命周期和线程安全缺失 |
| Q5 展示层影响 | ❌ | 整节缺失 |

**建议**：Q2 优先补充 T-4 runner 的失败日志和复现命令；Q4 把 `radix_sort` 的签名和线程安全约定显式写出来；Q5 至少补一段降级策略和可观测性指标。